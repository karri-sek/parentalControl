package com.bskyb.internettv.enums;

/**
 * This enum denotes the Parental Control levels in a specified order (ascending order)
 * The below values should not be shuffled - As the algorithm depends on the order.
 * Any new levels introduced should be places in the correct place.
 */
public enum ParentalControlLevel {
	 U, PG, TWELVE, FIFTEEN, EIGHTEEN;

}
