1. Followed the TDD approach.
2. Used EasyMock and JUnit for test cases
3. Followed the clean code approach - made sure that the documentation is less.
4. Enum defined for the ParentalControlLevel and the comparison of the enum constants (compareTo) is used.
6. run "mvn clean install" command
7. run "mvn clean test" command